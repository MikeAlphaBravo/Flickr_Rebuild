<h1>Check out these photos:</h1>

<ul class="list-group">
  <% @photos.each do | photo| %>
    <li><%= photo.title %></li>
    <li class="list-group-item"><%= image_tag(photo.image) %></li>
  <% end %>
</ul>
